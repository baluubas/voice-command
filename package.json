{
  "name": "voice-command",
  "version": "0.0.1",
  "description": "Node module for voice commands using native, offline speech recognizion.",
  "dependencies": {
    
  },
  "devDependencies": {},
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Anders Persson Söderling",
  "license": "MIT",

  "scripts": {
    
  }
}
